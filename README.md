# 💰 Gerenciamento de Receitas e Despesas

Uma aplicação web simples e intuitiva para gerenciamento de finanças pessoais, permitindo o cadastro de receitas e despesas com atualização automática do saldo.

Este projeto foi desenvolvido com foco no estudo aprofundado de **JavaScript (Vanilla)**, manipulando o DOM e utilizando métodos avançados de array para cálculos em tempo real.

## 🚀 Tecnologias Utilizadas

* **HTML5**: Estrutura semântica.
* **CSS3**: Estilização moderna e responsiva.
* **JavaScript (ES6+)**: Lógica de estado e manipulação de dados.

## 🧠 Conceitos e Aprendizados

Neste projeto, apliquei conceitos fundamentais de Engenharia de Software no Frontend:

* **Manipulação do DOM**: Criação e remoção dinâmica de elementos HTML (`createElement`, `appendChild`, `remove`).
* **High-Order Functions**: Uso intensivo de `map` (para listar valores), `filter` (para separar receitas/despesas e deletar itens) e `reduce` (para calcular os totais).
* **Persistência de Dados**: Uso da API `localStorage` para salvar os dados no navegador do usuário, mantendo o histórico mesmo após fechar a página.
* **JSON Parsing**: Conversão de Objetos para String (`JSON.stringify`) e vice-versa (`JSON.parse`) para armazenamento.
* **Gestão de Estado**: Separação entre a lógica dos dados (Array) e a camada visual (HTML).

## ✨ Funcionalidades

- [x] Adicionar novas transações (Receitas ou Despesas).
- [x] Remover transações existentes.
- [x] Cálculo automático de:
    - Total de Receitas.
    - Total de Despesas.
- [x] Persistência de dados local (não perde os dados ao atualizar a página).
- [x] Validação de formulário simples.

## 🛠️ Como Executar

1.  Clone este repositório:
    ```bash
    git clone [https://github.com/EstenioDantas/Gerenciamento-de-Receitas-e-Despesas.git](https://github.com/EstenioDantas/Gerenciamento-de-Receitas-e-Despesas.git)
    ```
2.  Abra a pasta do projeto no VS Code.
3.  Utilize o plugin **Live Server** ou abra o arquivo `index.html` diretamente no seu navegador.

---
Desenvolvido por [Estênio Dantas](https://github.com/EstenioDantas) 🚀
